// AUTO-GENERATED by scripts/audit/gen-prop-tests.mjs from scripts/audit/props/model.json.
// Do not hand-edit — rerun the generator. All assertion logic lives in
// ../test-driver (scripts/audit/props/test-driver.tsx).
import model from '../model.json';
import { runComponentPropTests } from '../test-driver';

const componentModel = model.components.find((c) => c.slug === "theme-scope");

if (!componentModel) {
  test.skip("theme-scope: no model entry", () => {});
} else {
  runComponentPropTests("theme-scope", componentModel);
}
